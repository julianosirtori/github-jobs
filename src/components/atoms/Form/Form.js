import styled from 'styled-components';
import {
  fontSize, fontWeight, space, layout,
} from 'styled-system';

const Form = styled.form`
  ${fontSize}
  ${fontWeight}
  ${space}
  ${layout}

  display: flex;
`;

export default Form;
