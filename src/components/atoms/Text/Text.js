import styled from 'styled-components';
import {
  color, fontSize, fontWeight,
} from 'styled-system';

// import {css}

const Text = styled.span`
  ${color}
  ${fontSize}
  ${fontWeight}
`;

export default Text;
